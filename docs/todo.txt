
TODO: make demo -> tool-tops with null as the index.
group ID==REf1t8
This is a v1 kata.
saver.group_joined() does not appear to be returning major_index polyfill.
$ curl --data '{"id":"REf1t8"}' --header 'Content-type: application/json' --request GET http://localhost:80/saver/group_joined | jq .
reveals this is true.

TODO: create a script that 
- creates group
- creates 3 avatars
- makes specific calls to saver for each avatar
- replace demo script with this so it opens a v2 dashboard

TODO: later, the script can be used to
- create tgz file 
- load tgz file into demos
- Note: these would be generally useful scripts for all microservices

TODO: update demo to show version2 group kata 

