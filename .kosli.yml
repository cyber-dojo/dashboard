version: 1

trail:
  attestations:
    - name: pull-request
      type: pull_request
  artifacts:
    - name: dashboard
      attestations:
        - name: provenance
          type: decision

        - name: sbom
          type: decision

        - name: snyk-container-scan
          type: decision

        - name: rubocop-lint
          type: junit
        - name: sonarcloud-scan
          type: sonar
        - name: unit-test
          type: junit
        - name: unit-test-coverage
          type: generic
